# web-sockets testing

run the following command to start the test server 
```
./test/run-autobahn.sh
```

run the following command to build and run the client
```
./test/run-client.sh
```

verify the output generated in tests/reports